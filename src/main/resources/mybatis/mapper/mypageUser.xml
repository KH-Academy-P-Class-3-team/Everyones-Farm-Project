<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Mypage">

	<select id="selectUser" resultType="Map">
		SELECT u.user_no,
		u.user_id,
		u.user_pw,
		u.user_name,
		u.email,
		u.phone,
		a.first_address
		FROM user_tb u,
		user_address a
		WHERE u.user_no = a.user_no
		AND u.user_no =#{userNo}
	</select>

	<select id="selectO3" resultType="O3">
		SELECT
			question_no,
			title,
			content,
			user_no,
			reg_date
		FROM QUESTION_ONEONONE
<<<<<<< HEAD
		ORDER BY question_no

	</select>

=======
		WHERE question_no =
		#{qNo}
	</select>
	<insert id="insertO3">
	INSERT INTO
		question_oneonone 
	VALUES
	(question_oneonone_seq.nextval, #{title}, #{content},  #{userNo} ,sysdate, 0)
	</insert>


	<select id="cntO3" resultType="int">
		select count(*) from
		Question_oneonone
		WHERE user_no=#{userNo}
	</select>

	<delete id="deleteOneonone">
	DELETE
		question_oneonone
	WHERE
		question_no = #{qNo}
	</delete>

	<select id="cntApli" resultType="int">
		select count(*) from APPLICATION
		where user_no=#{userNo}
	</select>


	<select id="selectActList" resultType="map">
		select apl.APPLICATION_NO,
		apl.ACTIVITY_NO,
		apl.USER_NO,
		apl.user_NAME,
		apl.user_id,
		apl.APPLICANT_PHONE,
		apl.APPLICANT_EMAIL,
		apl.PEOPLE,
		apl.ACTIVITY_DATE,
		apl.IS_GROUP,
		apl.IS_APPROVAL,
		fat.title,
		fat.is_Help,
		fat.farm_name,
		fat.phone
		from (select
		a.APPLICATION_NO,
		a.ACTIVITY_NO,
		a.USER_NO,
		a.APPLICANT_NAME,
		a.APPLICANT_PHONE,
		a.APPLICANT_EMAIL,
		a.PEOPLE,
		a.ACTIVITY_DATE,
		a.IS_GROUP,
		a.IS_APPROVAL,
		utb.user_name,
		utb.user_id
		from user_Tb utb, application a where a.user_no=utb.user_no) apl, (
		select
		fa.ACTIVITY_NO,
		fa.FARM_NO,
		fa.IS_HELP,
		fa.title,
		f.farm_name,
		f.PHONE
		from farm f, farm_activity fa
		WHERE f.farm_no = fa.farm_no)fat
		WHERE apl.activity_no = fat.activity_no
		AND apl.USER_NO = #{userNo}
	</select>

	<select id="selectOrderList" resultType="map">
		select
		ORDER_NO,
		OPTION_NO, USER_NO,
		FARMER_NO, ORDER_DATE, PAYMENT_STATUS,
		PAYMENT_PRICE, ORDER_AMOUNT, seller,
		NAME, AMOUNT
		from(
		select rownum
		rnum, n1.*
		from(
		select
		om.ORDER_NO, om.OPTION_NO, om.USER_NO,
		om.FARMER_NO, om.ORDER_DATE,
		om.PAYMENT_STATUS,
		om.PAYMENT_PRICE,
		om.ORDER_AMOUNT, om.name seller,
		p.NAME, p.AMOUNT
		from (
		select
		f.name,
		t.ORDER_NO, t.OPTION_NO,
		t.USER_NO, t.FARMER_NO, t.ORDER_DATE,
		t.PAYMENT_STATUS, t.PAYMENT_PRICE, t.ORDER_AMOUNT
		from
		farmer f,
		tb_order t where f.farmer_no=t.farmer_no) om , product_option p
		WHERE
		om.option_no = p.option_no
		AND
		om.user_no=#{userNo}
		ORDER BY
		om.ORDER_DATE desc) n1
		) WHERE rnum between #{page.start} and
		#{page.end}
		ORDER BY ORDER_NO desc
	</select>

	<select id="cntOrder" resultType="int">
		select count(*) from TB_ORDER
		where user_no=#{userNo}
	</select>
	
	<select id="orderminMax" resultType="map">
     select MIN(order_no) MIN, MAX(order_no) MAX
     from tb_order
     WHERE user_no = #{userNo}
	</select>

	<select id="minmax" resultType="map">
	 select Min(question_no) MIN, MAX(question_no) MAX
     from question_oneonone
     WHERE user_no = #{userNo}
	</select>

	<select id="cntBasket" resultType="int">
		select count(*) from basket WHERE user_no=#{userNo}
		AND purchase=0
	</select>

	<select id="selectBasketList" resultType="map">
		select
		basket_no, farmer_name, purchase, product_no, name, price, amount,
		option_name, delivery
		from(
		select rownum rnum, n1.*
		from(select
		b.basket_no, b.farmer_name, p.product_no, b.purchase,
		p.name, p.price, p.amount, p.option_name, p.delivery
		from (
		select
		pro.name, op.option_no, op.product_no,
		op.name option_name, op.price, op.amount, pro.DELIVERY
		from product pro, product_option op
		WHERE pro.PRODUCT_NO = op.PRODUCT_NO
		) p,
		(
		select
		name farmer_name, bas.basket_no, bas.user_no, bas.option_no, bas.purchase
		from farmer f, basket bas
		where f.farmer_no = bas.farmer_no) b
		where b.option_no = p.option_no AND b.user_no = #{userNo}
        AND purchase=0
		order by b.basket_no) n1 )
		WHERE rnum between #{page.start} and #{page.end}
	</select>

	<select id="selectOrderDetail" resultType="map">
		select
		odr.ORDER_NO,
		odr.OPTION_NO,
		odr.USER_NO,
		odr.FARMER_NO,
		odr.ORDER_DATE,
		odr.PAYMENT_STATUS,
		odr.PAYMENT_PRICE,
		odr.ZONECODE,
		odr.FIRST_ADDRESS,
		odr.SECOND_ADDRESS,
		odr.ORDER_AMOUNT,
		pro.OPTION_NAME,
		pro.OPTION_NO,
		pro.PRODUCT_NAME,
		pro.PRODUCT_NO
		from tb_order odr,
		(SELECT o.name option_name, o.option_no, p.name product_name, p.product_no
		from product_option o,product p
		WHERE p.product_no=o.product_no) pro
		WHERE odr.OPTION_NO = pro.OPTION_NO
		AND ORDER_NO = #{orderNo}
		ORDER BY odr.ORDER_NO
	</select>


	<delete id="deleteBasket">
		DELETE BASKET WHERE basket_no IN
		<foreach collection="array" item="arr" open="(" close=")"
			separator=",">
			#{arr}
		</foreach>
	</delete>
	
	<select id="emailCheck" resultType="USERTB">
	select * from user_tb where email = #{email}
	</select>

	<select id="phoneCheck" resultType="USERTB">
	select * from user_tb where phone = #{phone}
	</select>
	
	<select id="selectBasket" resultType="map">
	 SELECT 
	 	B.*, p.amount 
	 FROM
	 	BASKET B, PRODUCT_OPTION P 
	 WHERE
	 	B.option_no = p.option_no
     AND Basket_no IN
		<foreach collection="array" item="arr" open="(" close=")"
			separator=",">
			#{arr}
		</foreach>
	</select>
	
	<update id="subAmount">
	UPDATE 
		product_option
	SET
		amount=amount-1
	WHERE
		option_no=#{optionNo}
	</update>
	<update id="addAmount">
	UPDATE 
		product_option
	SET
		amount=amount+1
	WHERE
		option_no=#{optionNo}
	</update>
	<update id="updatePurchase">
	UPDATE
		basket
	SET
		purchase = 1
	WHERE
		basket_no = #{basketNo}
	</update>

	<update id="backPurchase">
	UPDATE
		basket
	SET
		purchase = 0
	WHERE
		basket_no = #{basketNo}
	</update>
	
	<select id="selectBasketPurchase" resultType="map">
	select
		b.basket_no, b.farmer_name, p.product_no, b.purchase,
		p.name, p.price, p.amount, p.option_name, p.delivery
		from (
		select
		pro.name, op.option_no, op.product_no,
		op.name option_name, op.price, op.amount, pro.DELIVERY
		from product pro, product_option op
		WHERE pro.PRODUCT_NO = op.PRODUCT_NO
		) p,
		(
		select
		name farmer_name, bas.basket_no, bas.user_no, bas.option_no, bas.purchase
		from farmer f, basket bas
		where f.farmer_no = bas.farmer_no) b
		where b.option_no = p.option_no AND b.user_no = #{userNo}
        AND purchase = 1
		order by b.basket_no
	</select>
	
<<<<<<< HEAD
>>>>>>> parent of 99d9b7a... mypageUser Without FarmerSession added
=======
>>>>>>> parent of 99d9b7a... mypageUser Without FarmerSession added
</mapper>
    
    